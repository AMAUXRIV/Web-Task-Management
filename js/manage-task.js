document.addEventListener('DOMContentLoaded',() => {

    //membuat objek dari class Task
    const myTasks = new Task();

    //membuat variable untuk  mengambil semua tasks
    const existingTasks = myTasks.getTasks();
})